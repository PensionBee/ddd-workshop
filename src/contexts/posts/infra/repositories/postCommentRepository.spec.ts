import { describe, expect, test } from "@jest/globals";

describe("postCommentRepository", () => {
  test("Should call `parsePostComment` on every interaction", () => {
    // TODO: complete me
    expect(true).toBe(false);
  });

  test("Should save a valid Post Comment and fetch it by it's ID", () => {
    // TODO: complete me
  });
});
